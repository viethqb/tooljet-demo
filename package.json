{
  "name": "tooljet-demo-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Test suite for pivot-table injection + backend pivot service",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
